apiVersion: v1
kind: Service
metadata:
  name: promptlab-api
  namespace: promptlab
  labels:
    app.kubernetes.io/name: promptlab
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: promptlab
    app.kubernetes.io/component: api
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
